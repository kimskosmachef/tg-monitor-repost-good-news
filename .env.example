# Секреты уровня Telegram-приложения — my.telegram.org → API development tools.
# Отдельно от config.yaml (§4, §10 docs/spec.md): аккаунт (session-файл) и
# ключи приложения — разные секреты с разным жизненным циклом.
# Реальный .env в git не попадает (см. .gitignore).

TG_API_ID=
TG_API_HASH=
